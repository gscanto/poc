# Projetos

**Data de Geração:** 2025-11-28 00:04:12

---

## Sumário

1. projetos

---


## projetos

1) Descrição da função do gt_detect()
2) Ajustemento dos parametros do detector
3) Implementacao do pipeline
# 2. Importar os librairies necessarios
from imutils import paths
import matplotlib.pyplot as plt

ap = argparse.ArgumentParser(description='Trabalho de Detectores') # Instancia argument parser

## Command line arguments ##
parser.add_argument('-i', '--images', required=True, type=str, help="path to input images directory")  # Iniciando uma variável vazia para armazenar novas imagens
args = vars(ap.parse_args())                                                # Armazena valores dessa variária
inputImagesDirectory = args['images']                                       # Atribui este varialble a referência à direta das imagens analissadas na entrada do script
print("\nInput Images Directory: {}".format(inputImagesDir))                # Imprimindo a referencia para o diretorio onde estamos mostrando os arquivos (image) analisados

gtDetectorsPath = "/home/pi/Desktop/ProjetoSidias/Databases/"            # Nome do ficheiro da base de dados contendo imagens complejas com detecção de objetos
groundTruthDetectionsCSVFileName = "GroundtruthDetectionResults.csv"       # Nomenclatura da fecha de acordo com qual dataframe queremos armazer na base de databasis
modelOutputTreshold = 0.5                                                   # Treshold para determinarificar se esse detectador foi confortante a partir desses resultados

dataFrameList = getDataFramesFromCsvFiles(fileNames=[groundTruthDetectionsCSVLane], gtdDetectorsBasePath=gtDetectersPath )           # Gera uma lista de vezes padronizadas do tipo DataFrame

if len(dataFrameLists)!=0 :                           # Verifica quantos vetores contientes nos lists de dadas
for i in range(len(dataSetColumns)):                  # Loop sobre cada coluna da base da dataset
dfDict = {columnName:[rowItem for rowIndex, rowItem in enumerate(entryRow)] for columnName, entryRow in zip(listOfAllColumndataFrameKeys,[entryRow[i] for entryRowInDataFrameList])}                   # Transforma cada linha da tabela into una dict. Dicionario tem as keys as column names and the values are those lines of each columns' entries
mergedDataFrame = pd.concat([pd.DataFrame(value, index=[index]) for key, value in dfDict.items()]).reset_index().rename({0:'ImageId','EncodedPixels':'ObjectBoundingBoxPixelCoords','IsOccluded':'occluded','IsDepicted':'depictionStatus','Truncated':'truncationStatus','AlphaChannel':'alphaMask','SegmentationClass':'classLabel','Difficult':'difficulty',"filename":"imgname",},axis=1)              # Merga cada tensore da base nas condicoes



---

## Referências

[1] Silva, M., Santos, R., "Fundamentos de Machine Learning", Journal of Artificial Intelligence Research, 2024.


[2] User Upload, "CV - gabriel.pdf", Upload via Interface, 2025.

